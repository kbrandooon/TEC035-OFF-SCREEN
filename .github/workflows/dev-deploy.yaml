name: Deploy to Development

on:
  push:
    branches:
      - development

jobs:
  call-central-workflow:
    uses: roii-tech/reusable-workflows/.github/workflows/react-deploy-dev.yml@main

    secrets:
      VITE_SUPABASE_URL: ${{ secrets.DEV_SUPABASE_URL }}
      VITE_SUPABASE_ANON_KEY: ${{ secrets.DEV_SUPABASE_ANON_KEY }}

      AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      AWS_S3_BUCKET: ${{ secrets.DEV_AWS_S3_BUCKET }}
      AWS_CLOUDFRONT_ID: ${{ secrets.DEV_AWS_CLOUDFRONT_ID }}

      GOOGLE_CHAT_URL: ${{ secrets.GOOGLE_CHAT_URL }}
