---
description:
---

Now, before I commit, please verify all of this, only in the changed files:

- Test the new feature thoroughly (database logic, API calls, and UI components).
- Ensure all components have functionallity.
- Avoid import or select \*.
- API's files has to be one function by file.
- Make sure to make as few API calls as possible, only those that are necessary.
- Ensure follow screaming architecture.
- Remember that ALL FEATURES must be documented in its corresponding scope.
- Check for existing components and reuse them if possible or Add it to one of the global folders if you think it might be a reusable component.
- Verify that the local database has not been modified

Also, check the components, APIs, hooks, or any existing files and see if any can be reused and put them in your global folder, but notify me first.

- Remember that ALL FEATURES must be documented in its corresponding scope.
