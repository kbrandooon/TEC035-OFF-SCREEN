---
trigger: always_on
---

follow colors, typhografy, colors, design patterns and design of the feature "contra-recibos", "all_trips", You must verify that the component you will use check if exist in src/components/\*, if no component do not exists download from shacdn.

When finish a request you have to ask por documentation, with the skill "expert-documentation update CHANGELOG.md and documentation/scopes

You must not execute "supabase db push"

This project is from flutterflow and in this we're doing a rebuild so the implementations could be better.

Every import must be with alias "@"
